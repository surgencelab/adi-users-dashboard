node_modules
dist
tsconfig.tsbuildinfo
__pycache__/
*.pyc

# The whole raw index is regenerable and is persisted by the CI cache, not by
# git. Committing part of it (a checkpoint without the log file it points at)
# leaves a fresh checkout in an inconsistent state.
data/

legacy-vite/node_modules
legacy-vite/dist

.DS_Store
.env

data.json.candidate
.vercel
